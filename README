mpd-watchdog
============

Monitor the Music Player Daemon (MPD) and restart playback when it
appears to hang.  This can be especially useful when playing internet
streams.

Originally based on [http://mpd.wikia.com/wiki/Hack:stream-monitor].
